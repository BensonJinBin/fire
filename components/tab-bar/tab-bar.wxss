/* components/tab-bar/tab-bar.wxss */
.custom-bottom-nav {
  position: fixed;
  bottom: 30rpx;
  left: 50rpx;
  right: 50rpx;
  background-color: #000;
  height: 100rpx;
  display: flex;
  justify-content: space-around;
  align-items: center;
  padding: 15rpx 0;
  z-index: 1000;
  border-radius: 20rpx;
  box-shadow: 0 4rpx 20rpx rgba(0, 0, 0, 0.3);
}

.nav-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #fff;
  font-size: 28rpx;
  padding: 5rpx;
  transition: all 0.3s ease;
}

.nav-item.active {
  color: #F5A623;
}

.nav-item.active .nav-icon {
  filter: brightness(0) saturate(100%) invert(74%) sepia(58%) saturate(571%) hue-rotate(359deg) brightness(97%) contrast(89%) !important;
}

.nav-item .nav-icon {
  width: 60rpx;
  height: 60rpx;
  margin-bottom: 4rpx;
  filter: brightness(0) invert(1);
}

.nav-item .nav-text {
  font-size: 24rpx;
  line-height: 1;
}
